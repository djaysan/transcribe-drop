#!/bin/bash
# Double-click this in Finder to set up Transcribe Drop (installs tools + builds the app).
# First time: right-click → Open (it's an unsigned local script).
cd "$(dirname "$0")"
./install.sh
echo ""
echo "You can close this window."
